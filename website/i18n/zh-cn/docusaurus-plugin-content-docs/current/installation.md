---
sidebar_position: 2
title: 安装
---

# 安装

## 前置条件

安装 Python 3.9 或以上版本。

## 安装 CLI

执行以下命令进行安装：

```bash
pip install alibabacloud-ros-tran
```

## 安装 Web 服务（可选）

若要通过 `rostran server start` 使用浏览器版 playground，请安装可选的 `serve` 依赖：

```bash
pip install "alibabacloud-ros-tran[serve]"
```
