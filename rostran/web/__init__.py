"""Web service for rostran, exposing transform/format capabilities over HTTP.

The web service is optional and only available when the ``serve`` extra is
installed::

    pip install "alibabacloud-ros-tran[serve]"

It is started via the CLI::

    rostran serve
"""
